# 🎙️ 음성 캘린더 — 사용 설명서

말로 "**7월 24일 15시에 김대리 상담 기록해줘**"라고 하면 구글 캘린더에 자동 등록되는 휴대폰용 웹앱입니다. 별도 앱 설치나 스토어 심사 없이, 휴대폰 홈화면에 아이콘으로 추가해 앱처럼 쓸 수 있습니다.

---

## 구성 파일

| 파일 | 역할 |
|------|------|
| `index.html` | 앱 본체 (음성 인식 + 날짜 파싱 + 캘린더 등록) |
| `manifest.json` | 홈화면 설치 정보 |
| `sw.js` | 오프라인 캐시(서비스워커) |
| `icon-192.png`, `icon-512.png` | 앱 아이콘 |

이 다섯 파일을 **같은 폴더**에 함께 두어야 정상 작동합니다.

---

## 준비: 한 번만 하면 됩니다 (약 10분)

음성 인식은 별도 설정이 필요 없지만, **구글 캘린더에 등록하려면 "구글 클라이언트 ID"가 필요**합니다. 아래 순서대로 발급받으세요.

### 1단계 — 구글 클라우드 프로젝트 만들기

1. https://console.cloud.google.com 접속 → 구글 로그인
2. 상단 프로젝트 선택 → **새 프로젝트** → 이름(예: `음성캘린더`) → 만들기

### 2단계 — 캘린더 API 켜기

1. 왼쪽 메뉴 **API 및 서비스 → 라이브러리**
2. `Google Calendar API` 검색 → **사용 설정**

### 3단계 — OAuth 동의 화면 설정

1. **API 및 서비스 → OAuth 동의 화면**
2. User Type: **외부** → 만들기
3. 앱 이름(예: `음성캘린더`), 사용자 지원 이메일, 개발자 이메일 입력 → 저장
4. **범위(Scopes)**는 건너뛰어도 됩니다
5. **테스트 사용자**에 본인 지메일(wonsuckyang@gmail.com) 추가 → 저장

> 테스트 사용자로 등록된 계정만 로그인할 수 있습니다. 본인 계정을 꼭 추가하세요.

### 4단계 — OAuth 클라이언트 ID 발급

1. **API 및 서비스 → 사용자 인증 정보**
2. **+ 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**
3. 애플리케이션 유형: **웹 애플리케이션**
4. **승인된 자바스크립트 원본**에 앱을 올릴 주소를 추가 (아래 배포 방법 참고)
   - 예: `https://내계정.github.io`
   - 테스트용으로는 `http://localhost:8000` 도 추가
5. 만들기 → 나오는 **클라이언트 ID** 복사 (`...apps.googleusercontent.com` 형태)

### 5단계 — 클라이언트 ID를 앱에 입력

`index.html`을 메모장 등으로 열어 상단의 이 줄을 찾습니다:

```js
const CLIENT_ID = "여기에_클라이언트_ID_붙여넣기.apps.googleusercontent.com";
```

복사한 값으로 교체 후 저장하세요.

---

## 배포: 휴대폰에서 쓰려면 인터넷 주소가 필요합니다

음성 인식과 구글 로그인은 보안상 **https 주소**에서만 동작합니다. 아래 중 편한 방법 하나를 고르세요.

**방법 A — GitHub Pages (무료, 추천)**
1. github.com 가입 → 새 저장소(repository) 생성
2. 다섯 파일 업로드
3. 저장소 Settings → Pages → Branch를 `main`으로 지정 → 저장
4. 몇 분 뒤 `https://내계정.github.io/저장소이름/` 주소가 생성됩니다
5. 이 주소를 위 4단계 "승인된 자바스크립트 원본"에도 추가하세요

**방법 B — Netlify (무료)**
- netlify.com 접속 → 다섯 파일이 든 폴더를 화면에 끌어다 놓으면 즉시 https 주소가 생성됩니다

**방법 C — 컴퓨터에서 테스트만**
```
python -m http.server 8000
```
브라우저에서 `http://localhost:8000` 접속 (원본 목록에 localhost:8000 등록 필요)

---

## 휴대폰 홈화면에 앱으로 추가

- **안드로이드(Chrome)**: 배포한 주소 접속 → 메뉴(⋮) → **홈 화면에 추가**
- **아이폰(Safari)**: 주소 접속 → 공유 버튼 → **홈 화면에 추가**

이제 홈화면 아이콘을 누르면 전체화면 앱처럼 실행됩니다.

---

## 사용 방법

1. 앱을 열고 우측 상단 **로그인** → 구글 계정 연결(최초 1회)
2. 가운데 🎤 버튼을 누르고 말합니다
   - 예) "**8월 3일 오후 2시에 행정심판 준비 기록해줘**"
3. 인식된 내용으로 **제목·날짜·시간**이 자동 채워집니다 → 확인/수정
4. **캘린더에 등록** 버튼 → 구글 캘린더에 저장 완료

### 말하는 방식 예시

| 말 | 결과 |
|----|------|
| 7월 24일 15시에 김대리 상담 기록해줘 | 7/24 15:00 · "김대리 상담" |
| 내일 오전 10시 30분에 출입국 상담 기록해줘 | 내일 10:30 · "출입국 상담" |
| 9월 15일 오후 4시 반에 산재 서류 검토 기록해줘 | 9/15 16:30 · "산재 서류 검토" |
| 오늘 3시에 팀 회의 등록해줘 | 오늘 03:00 · "팀 회의" |

인식 규칙:
- 날짜: `N월 N일`, 또는 `오늘/내일/모레`
- 시간: `N시`, `N시 M분`, `N시 반`, `오전/오후/저녁` 구분
- 시간을 안 말하면 오전 9시, 오전/오후를 안 말하면 말한 숫자 그대로 (등록 화면에서 수정 가능)

---

## 참고 · 주의

- **브라우저**: 음성 인식은 **Chrome(안드로이드)** 에서 가장 안정적입니다. 아이폰 Safari도 지원하나 인식률이 다소 낮을 수 있습니다.
- **개인정보**: 음성은 브라우저(구글) 음성 인식 서비스로 처리되며, 일정은 본인 구글 캘린더에만 저장됩니다. 별도 서버로 전송하지 않습니다.
- **소요 시간**: 기본 60분으로 등록됩니다. 등록 화면에서 조정하세요.
- **시간대**: 한국 시간(Asia/Seoul) 기준으로 저장됩니다.
- 항상 **등록 전에 화면에서 내용을 한 번 확인**하도록 설계했습니다. 잘못 인식된 부분은 손으로 고칠 수 있습니다.
